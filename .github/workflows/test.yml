name: Test
on:
  push:
    branches:
      - master

jobs:
  build:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: list files
        run: ls
      - name: install dependencies
        run: npm i
      - name: run test
        run: npm run test
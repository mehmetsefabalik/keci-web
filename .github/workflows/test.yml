name: Test
on:
  push:
    branches:
      - master

jobs:
  build:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: working directory
        run: pwd
      - name: list files
        run: ls -la
      - name: install dependencies
        run: npm i
      - name: run test
        run: npm run test
